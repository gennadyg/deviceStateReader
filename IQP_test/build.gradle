apply plugin: 'java'


group = 'com.iqp'
archivesBaseName = 'relatedDataAPI'

def localBuidNo = "1"

println 'Generated artifact version: ' + version


project.description = 'IQP test'



repositories
{
	mavenLocal()
	mavenCentral()
	maven { url "http://maven.restlet.org" }
}
dependencies 
{

	
	compile 'org.simpleframework:simple-xml:2.7.1'
	 	
	compile 'org.apache.httpcomponents:httpclient:4.3.2'
	compile 'org.apache.commons:commons-io:1.+'

	compile 'org.apache.httpcomponents:httpclient:4.3.2'
	    
    compile 'log4j:log4j:1.2.17'

  	
  	compile 'mysql:mysql-connector-java:5.1.32'
}
def manifestClasspath = configurations.compile.collect{ 'lib/' + it.getName() }.join(' ')
println manifestClasspath
jar.doFirst{	 
   manifest {
            attributes("Manifest-Version": "1.0",
		                "Main-Class"             : "com.iqp.DeviceInfoReader",
		                "Class-Path"             :  manifestClasspath
            )
            }
}
